select /* +PARALLEL*/
 A.CMS_ID,
 A.PAN,
 A.CREATE_DATE,
 A.CANCEL_DATE,
 a.account,
 A.PRODUCT_NAME,
 CASE
   WHEN A.CANCEL_DATE < SYSDATE THEN
    'CLOSED'
   ELSE
    'OPEN'
 END AS "STATUS"
  from cms.d_card A
 INNER JOIN DWH.D_CUSTOMER B
    ON A.CMS_ID = B.CMS_ID
 WHERE B.PIN = '60CFME6';
