select /* +parallel*/
 unique b.pin,B.NAME,
 b.vezife,
 a.start_date,
 a.maturity_date,
 a.contract_ref_no,
 ROUND(a.initial_amount) AMOUNT
  from dwh.D_CONTRACT_DEPOSIT a
 inner join fraud.emp_01_05_2023 b
    on a.cif = b.cif
 where a.Close_Date IS NULL
 ORDER BY AMOUNT DESC OFFSET 10 ROWS;