select * 
from dvs.d_dvs_public_person_orders_steps a 
inner join dvs.f_online_account_open b on a.order_id=b.order_id
inner join dwh.d_contract_credit c on  b.cif=c.cif
where a.steps_status ='rejected'
and to_date(b.order_create_date,'dd.mm.yyyy') =to_date(c.create_date,'dd.mm.yyyy');